{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://example.com/schemas/tools/render.capture.viewport.result.schema.json",
  "title": "render.capture.viewport result",
  "allOf": [
    { "$ref": "../dispatch-result.schema.json" },
    {
      "type": "object",
      "properties": {
        "tool": { "const": "render.capture.viewport" },
        "approval_class": { "const": "read_only" },
        "execution_mode": {
          "type": "string",
          "enum": ["simulated", "real"]
        },
        "simulated": { "type": "boolean" }
      }
    }
  ]
}
