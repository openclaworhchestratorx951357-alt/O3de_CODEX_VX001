{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://example.com/schemas/tools/gem.enable.artifact-metadata.schema.json",
  "title": "gem.enable artifact metadata",
  "allOf": [
    {
      "$ref": "./gem.enable.execution-details.schema.json"
    },
    {
      "type": "object",
      "required": [
        "tool",
        "agent",
        "execution_mode",
        "adapter_family",
        "adapter_mode",
        "adapter_contract_version"
      ],
      "properties": {
        "tool": { "const": "gem.enable" },
        "agent": { "const": "project-build" },
        "execution_mode": { "const": "simulated" }
      }
    }
  ]
}
