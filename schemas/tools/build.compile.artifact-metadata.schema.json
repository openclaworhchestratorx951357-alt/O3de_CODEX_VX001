{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://example.com/schemas/tools/build.compile.artifact-metadata.schema.json",
  "title": "build.compile artifact metadata",
  "allOf": [
    {
      "$ref": "./build.compile.execution-details.schema.json"
    },
    {
      "type": "object",
      "required": [
        "tool",
        "agent",
        "execution_mode",
        "adapter_family",
        "adapter_mode",
        "adapter_contract_version"
      ],
      "properties": {
        "tool": { "const": "build.compile" },
        "agent": { "const": "project-build" },
        "execution_mode": { "enum": ["simulated", "real"] }
      }
    }
  ]
}
