{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://example.com/schemas/tools/build.configure.artifact-metadata.schema.json",
  "title": "build.configure artifact metadata",
  "allOf": [
    {
      "$ref": "./build.configure.execution-details.schema.json"
    },
    {
      "type": "object",
      "required": [
        "tool",
        "agent",
        "execution_mode",
        "adapter_family",
        "adapter_mode",
        "adapter_contract_version"
      ],
      "properties": {
        "tool": { "const": "build.configure" },
        "agent": { "const": "project-build" },
        "execution_mode": {
          "type": "string",
          "enum": ["simulated", "real"]
        }
      }
    }
  ]
}
